# Rust build artifacts
/target/
**/target/

# Secrets — must never be committed
.env*
!.env.example

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# OS junk
.DS_Store
Thumbs.db

# Runtime Redis data — the bind mount target written by the redis service.
# Never commit operator runtime state into the repo.
redis_data/
